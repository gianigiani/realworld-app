import { AppAuthDirective } from './app-auth';

describe('AppAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new AppAuthDirective();
    expect(directive).toBeTruthy();
  });
});
